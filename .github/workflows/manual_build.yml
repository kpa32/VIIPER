name: Manual Build (Dev)

on:
    workflow_dispatch:

permissions:
    contents: read

jobs:
    build:
        uses: ./.github/workflows/build_base.yml
        secrets: inherit
        with:
            upload_artifacts: true
